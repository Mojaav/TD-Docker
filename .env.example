# Variables d'environnement pour Docker Compose
# Copiez ce fichier en .env et modifiez les valeurs

# Base de données
DB_USER=postgres
DB_PASSWORD=monsuperpassword
DB_NAME=mydb

# Registre Docker (pour le push des images)
DOCKER_REGISTRY=docker.io
DOCKER_USERNAME=monuser
IMAGE_TAG=latest
